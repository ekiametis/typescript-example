version: '3'

networks: 
  my_stack_network:
    driver: bridge
    ipam:
      driver: default

services:
  api:
    build: .
    networks:
      - my_stack_network
    ports:
      - "8080:8080"
    environment:
      PORT: 8080